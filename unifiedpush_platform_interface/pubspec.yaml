name: unifiedpush_platform_interface
description: A common platform interface for the unifiedpush plugin.
repository: https://github.com/UnifiedPush/flutter-connector
issue_tracker: https://github.com/UnifiedPush/flutter-connector/issues
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.0

dev_dependencies:
